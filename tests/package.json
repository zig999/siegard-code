{
  "name": "siegard-code-tests",
  "version": "1.0.0",
  "description": "Test suite for dist/ agent infrastructure",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run --reporter=json --outputFile=./results.json"
  },
  "devDependencies": {
    "ajv": "^6.12.6",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0",
    "vitest": "^1.6.0"
  }
}
