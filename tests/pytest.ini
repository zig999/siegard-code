[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: long-running tests (>5s — e.g. circuit-breaker 50-event integration). Fast inner loop: pytest -m "not slow"
