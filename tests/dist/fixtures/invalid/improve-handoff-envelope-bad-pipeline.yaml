# INVALID: execution_policy.pipeline must be lean or full
handoff_envelope:
  id: IMPROVE-20260418-190000
  source: u-improve
  invocation_source: u-improve
  improve_session: bad-pipeline
  improvement_task: "Some change"
  mode_hint: "fast-track:patch"
  affected_specs: []
  estimated_task_contracts: 1
  execution_policy:
    pipeline: express
    regression_test_required: false
    planner_required: false
  return_contract:
    write_to: "{SESSIONS_DIR}/bad-pipeline/log-orchestrator-dev.md"
    update_field: spec_change_status
    expected_terminal_states: [completed, failed]
