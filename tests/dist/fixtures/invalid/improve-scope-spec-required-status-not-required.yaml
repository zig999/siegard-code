# IMPV-002 violation: spec_change_required must have completed or divergence_accepted, not not_required
improve_scope:
  description: "Add filters to transaction list"
  type: spec_change_required
  spec_change_status: not_required
  affected_specs:
    - path: "front/features/transactions.feature.spec.md"
      sections: ["§2"]
      change_summary: "New filter states"
  estimated_task_contracts: 1
  planner_required: false
  planner_skip_reason: "Single TC, single spec"
