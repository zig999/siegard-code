# VALID: visual-only fix (former u-bug-report "visual" type)
# pipeline: lean → Developer direct, no Planner, no TDD
handoff_envelope:
  id: IMPROVE-20260418-160000
  source: u-improve
  invocation_source: u-improve
  improve_session: visual-tweaks
  improvement_task: "Save button misaligned on checkout form — shift 4px to match field axis"
  mode_hint: "fast-track:patch"
  affected_specs: []
  estimated_task_contracts: 1
  execution_policy:
    pipeline: lean
    regression_test_required: false
    planner_required: false
  return_contract:
    write_to: "{SESSIONS_DIR}/visual-tweaks/log-orchestrator-dev.md"
    update_field: spec_change_status
    expected_terminal_states: [completed, failed]
