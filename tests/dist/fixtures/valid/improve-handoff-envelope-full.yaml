handoff_envelope:
  id: IMPROVE-20260418-090000
  source: u-improve
  invocation_source: u-improve
  improve_session: auth-rewrite
  improvement_task: "Replace session-token storage to meet new compliance requirements"
  mode_hint: "full"
  affected_specs:
    - path: "domains/auth/auth.spec.md"
      sections: ["§3", "§5"]
      change_summary: "Modify session token contract — breaking change"
    - path: "domains/auth/back/auth.back.md"
      sections: ["§4"]
      change_summary: "Update token storage business rule"
  estimated_task_contracts: 3
  execution_policy:
    pipeline: full
    regression_test_required: true
    planner_required: true
  return_contract:
    write_to: "{SESSIONS_DIR}/auth-rewrite/log-orchestrator-dev.md"
    update_field: spec_change_status
    expected_terminal_states: [completed, failed]
