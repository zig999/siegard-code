# VALID: full pipeline but no regression test required (declarative/spec-only change)
handoff_envelope:
  id: IMPROVE-20260418-170000
  source: u-improve
  invocation_source: u-improve
  improve_session: spec-clarifications
  improvement_task: "Clarify wording of BR-04 description — no logic change"
  mode_hint: "fast-track:patch"
  affected_specs:
    - path: "domains/billing/back/billing.back.md"
      sections: ["§4"]
      change_summary: "Rewrite BR-04 description for clarity (no semantic change)"
  estimated_task_contracts: 1
  execution_policy:
    pipeline: full
    regression_test_required: false
    planner_required: false
  return_contract:
    write_to: "{SESSIONS_DIR}/spec-clarifications/log-orchestrator-dev.md"
    update_field: spec_change_status
    expected_terminal_states: [completed, failed]
