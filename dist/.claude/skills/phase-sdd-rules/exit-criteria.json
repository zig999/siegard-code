{
  "phase": "sdd",
  "version": "1.2",
  "criteria": [
    {
      "id": "handoff_manifest_approved",
      "description": "handoff-manifest.yaml passes u-handoff-validator (13 rules + sha256) AND is marked Status: approved",
      "script": "scripts/check_handoff_manifest_approved.py",
      "required": true,
      "blocking": true,
      "applies_to_modes": [
        "standard",
        "targeted"
      ]
    },
    {
      "id": "all_domains_validated",
      "description": "No INVALID status in any file under SPECS_DIR/_validation/",
      "script": "scripts/check_all_domains_validated.py",
      "required": true,
      "blocking": true,
      "applies_to_modes": [
        "standard"
      ]
    },
    {
      "id": "all_improve_reviewers_completed",
      "description": "Every sdd_improve_*_spec-reviewer task reached terminal status completed (targeted-mode substitute for all_domains_validated)",
      "script": "scripts/check_all_improve_reviewers_completed.py",
      "required": true,
      "blocking": true,
      "applies_to_modes": [
        "targeted"
      ]
    },
    {
      "id": "error_codes_synced",
      "description": "All error.code values found in in-scope spec files are registered in SPECS_DIR/error-codes.md (scoped to touched domains on /u-improve via --workflow-id; global for u-spec/greenfield)",
      "script": "scripts/check_error_codes_synced.py",
      "required": true,
      "blocking": true,
      "applies_to_modes": [
        "standard",
        "targeted"
      ]
    },
    {
      "id": "sdd_artifacts_committed",
      "description": "SIEGARD-05 \u2014 every spec artifact listed in handoff-manifest.yaml (backend_package/frontend_package paths, compliance_report, feature/flow paths) is git-tracked with no uncommitted changes. The Orchestrator-SDD commits the generated specs before phase_exit_approved so they are never lost as untracked files.",
      "script": "scripts/check_sdd_artifacts_committed.py",
      "required": true,
      "blocking": true,
      "applies_to_modes": [
        "standard",
        "targeted"
      ]
    },
    {
      "id": "spec_drift_reviewed",
      "description": "R04d \u2014 when sdd_policy.drift_check is 'warn' or 'required' in .orch/config.json, a current /u-drift report must exist for the approved specs (spec_content_hash matching the specs on disk). Opt-in and OFF by default: /u-drift costs an LLM code-inventory pass, and a gate that worsens the measured cost problem would be switched off wholesale rather than used where it matters. Vacuously met when disabled.",
      "script": "scripts/check_spec_drift_reviewed.py",
      "required": true,
      "blocking": true,
      "applies_to_modes": [
        "standard",
        "targeted"
      ]
    }
  ]
}
