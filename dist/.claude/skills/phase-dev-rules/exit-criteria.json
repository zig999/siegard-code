{
  "phase": "dev",
  "version": "1.0",
  "criteria": [
    {
      "id": "all_impl_tasks_terminal",
      "description": "All dev-phase tasks have reached a terminal status (completed or dlq). DLQ is accepted-terminal for dev (retries exhausted) and surfaced via evidence.dlq_blocks_criterion; critical-tier DLQ is escalated by the meta-orchestrator (E04). Contrast: the test phase blocks on DLQ.",
      "script": "scripts/check_all_impl_tasks_terminal.py",
      "required": true,
      "blocking": true
    },
    {
      "id": "all_deliveries_qa_ready",
      "description": "Every delivery.md artifact from completed dev tasks contains qa_ready: true",
      "script": "scripts/check_all_deliveries_qa_ready.py",
      "required": true,
      "blocking": true
    },
    {
      "id": "no_open_prohibitions",
      "description": "No delivery.md artifact contains a non-empty prohibition_violations list",
      "script": "scripts/check_no_open_prohibitions.py",
      "required": true,
      "blocking": true
    }
  ]
}
